import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render(
  <React.StrictMode>
    <div className="bg-gradient-to-r from-green-900 to-teal-200 min-h-screen">

    <App />
    </div>
  </React.StrictMode>,
  document.getElementById('root')
);